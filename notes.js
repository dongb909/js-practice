// for (var key in obj) {
//     var keyValue = obj[key]
// }


// Object.keys(obj) // array of the object's keys

// var sampleArr = [1,2,3,45,54]
// sampleArr.slice(0,3) // [1,2,3]
// // sampleArr = [1,2,3,45,54]

// sampleArr.splice(0,3) // [1,2,3]
// // sampleArr = [45, 54]
// handy for when you're using the entire array
// keep spslicing and operating until exisiting array is empty